version: '3.8'

services:


  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    depends_on:
      - backend
    networks:
      - time-management-network

networks:
  time-management-network:
    driver: bridge

volumes:
  backend-data: